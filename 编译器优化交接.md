# 编译器优化交接

更新时间：2026-05-24 18:30

## 当前目标

当前在做 SysY2022 到 64 位 RISC-V 汇编的编译器性能优化。由于本地没有 RISC-V 运行环境，本地只做源码修改和静态检查，性能结果依赖提交到测评平台后反馈的执行时间。

当前主工作目录：

- 当前提交版本源码：`/home/vance/Compiler/目标代码生成`
- 平台要求：`/home/vance/Compiler/平台题目要求.md`
- 优化归档：`/home/vance/Compiler/优化归档`
- 优化日记：`/home/vance/Compiler/优化归档/优化日记.md`

重要约束：

- 不在编译器代码中调用外部编译器、汇编器、链接器、脚本或 shell。
- 不加入 `system`、`popen`、`fork`、`exec` 等外部工具调用逻辑。
- 不针对特定用例名、文件名、函数名、固定输出或评测平台信息做特化优化。
- 本地验证不运行 `gcc`、`clang`、`make`、`flex`、`bison`、RISC-V 工具链，也不运行 `./compiler`。
- `目标代码生成/` 应始终代表当前准备提交平台的版本；历史版本放到 `优化归档/`。

## 已保留的优化记录

### 2026-05-22 14:22 控制流/短路优化

归档目录：

- `优化归档/2026_5_22_1600_2289.3332s`

平台时间：

- `2289.3332s`

主要修改：

- 短路逻辑表达式 `&&` / `||` 不再通过临时栈槽保存布尔值，改为直接在分支标签中设置 `a0 = 1/0`。
- 条件跳转识别可静态求值的整数常量条件，直接跳转到 true/false 标签。
- 删除 `while` 入口处跳到紧随其后的条件标签的冗余跳转。

结论：

- 该记录是目前保留的基准优化结果。

### 2026-05-22 17:00 基本块内 DAG 优化

归档目录：

- `优化归档/2026-5-22-1700-2292.3322s`

平台时间：

- `2292.3322s`

主要修改：

- 在 `目标代码生成/compiler.c` 的目标代码生成阶段加入基本块内三地址值建模和 DAG 优化。
- 只处理连续的本地 `int` 标量赋值语句。
- 对数组、全局变量、函数调用、浮点、短路逻辑、控制流和 I/O 语句保守回退。
- 已覆盖常量折叠、代数化简、公共子表达式复用、同一基本块内覆盖赋值消除。

结论：

- 平台结果比上一条保留记录慢约 `2.9990s`。
- 初步判断可能是 DAG 生成路径引入了过多临时栈槽和额外 `lw/sw`，收益没有抵消开销。
- 下一步应优先分析或回退这部分 DAG 路径，避免继续在变慢版本上叠加优化。

## 已删除的记录

曾经有一条 `2026-05-22 17:00 imm==0 地址加法冗余消除优化记录`，后来确认没有有效归档、代码也找不到，且性能无明显提升，因此已从优化日记中删除。

## 当前工作区状态提示

最近一次检查时，工作区有这些状态：

- `目标代码生成/compiler.c`：已修改，包含当前 DAG 优化代码。
- `目标代码生成/compiler.zip`：删除状态。
- `优化归档/优化日记.md`：已修改，包含最新记录。
- `优化归档/2026-5-22-1700-2292.3322s/`：未跟踪目录，包含最新平台归档的 `compiler.zip`。

新对话开始后，建议先运行：

```bash
git status --short
find 优化归档 -maxdepth 2 -printf '%y %p\n'
sed -n '1,220p' 优化归档/优化日记.md
```

## 下一步建议

优先方向：

1. 不建议继续直接叠加当前 DAG 优化，因为平台结果已变慢。
2. 可以先回看 `目标代码生成/compiler.c` 中新增的 DAG 路径，重点检查是否大量使用临时栈槽保存中间节点和最终结果。
3. 如果要保留 DAG 思路，应改成更轻量的寄存器/表达式级复用，或者只在确定有重复复杂表达式时启用。
4. 如果目标是尽快恢复性能，应考虑回退 DAG 大改，只保留 14:22 那次控制流/短路优化基线。
5. 每次平台测完后，把提交包放入 `优化归档/<日期时间_秒数>/`，并更新 `优化归档/优化日记.md`。

## 给新对话的简短背景

请接着 `/home/vance/Compiler` 的编译器优化工作。当前项目是 SysY2022 到 RISC-V 汇编编译器，主源码在 `目标代码生成/`。本地不能运行 RISC-V，也不要调用外部编译器或运行 `./compiler`，只能静态修改和检查。优化结果由用户提交平台反馈秒数。当前 DAG 基本块优化版本平台时间 `2292.3322s`。
