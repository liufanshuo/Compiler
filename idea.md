# LLVM->RISC-V 性能优化建议

当前平台运行时间：3495s。已经 AC，后续优化应以“保持正确性基线”为第一原则，每次只引入一个通用优化，并用完整测试回归确认。

## 1. 优先做 RISC-V 后端的局部寄存器缓存

当前 `rv_emit_int_binary()`、`rv_emit_load_inst()`、`rv_emit_store_inst()` 等代码基本是：

1. 从栈槽 `lw/ld/flw` 读操作数；
2. 用 `t0/t1/t2` 或 `ft0/ft1/ft2` 计算；
3. 立刻 `sw/sd/fsw` 写回栈槽。

这会让循环内每个临时值都产生大量内存访问，是目前最明显的性能瓶颈。

建议先做一个基本块内的轻量寄存器缓存：

- 对最近生成的 IR 临时值记录其所在物理寄存器。
- 如果后续指令马上使用该值，直接复用寄存器，不从栈重新加载。
- 在遇到 `call`、基本块结束、分支跳转前，把仍需保留的值写回栈槽。
- 第一版只缓存 `i32` 临时值，使用 `t0-t6`，不要跨基本块。

收益预期：高。风险可控，因为不改变 IR，只减少冗余 load/store。

## 2. 删除无用的临时栈槽写回

很多 IR 结果只被使用一次，且使用点紧跟定义点。这类值没有必要写入栈槽。

可以先实现 use-count：

- 在 `rv_prepare_frame()` 扫描函数时统计每个 `IRValue` 的使用次数。
- 对 use-count 为 1 且定义和使用在同一基本块内的值，允许直接寄存器传递。
- 对 `alloca`、函数参数、跨块值、call 返回值先保守保留栈槽。

收益预期：高。尤其对循环里的算术、比较、GEP 地址计算很明显。

## 3. 优化 GEP 地址计算

当前 `rv_emit_gep()` 每个下标都会：

- 加载 index；
- `li stride`；
- `mul index, index, stride`；
- `add base, base, index`。

对数组访问密集的性能用例，这部分成本很高。

建议：

- stride 为 4、8、16、32 等 2 的幂时，用 `slli` 代替 `li + mul`。
- index 为常量时，直接把 `index * stride` 合并为立即数偏移。
- 连续访问 `A[i]`、`A[i+1]` 的循环后续可做指针递增，但第一阶段先不做，避免破坏语义。

收益预期：中到高。风险低。

## 4. 基本块内常量折叠和代数化简

IR 生成阶段已经有部分常量处理，但 RISC-V 后端仍可做简单 peephole：

- `add x, 0` 直接移动或复用原值。
- `sub x, 0` 直接复用原值。
- `mul x, 1` 直接复用原值。
- `mul x, 0` 直接生成 0。
- `div x, 1` 直接复用原值。
- 比较布尔值后又 `andi value, 1` 的场景可以省掉重复归一化。

收益预期：中。实现简单，但要注意不要改变有符号除法、取模和浮点语义。

## 5. 循环不变量外提

性能用例大量循环访问全局变量、常量维度和数组基址。可以在 IR 层或后端层做保守外提：

- 对循环内反复出现的全局数组基址 `lla`，如果循环内不改变该全局符号地址，则外提到循环前。
- 对 `N`、`KSIZE` 这种 `const int` 全局常量，尽量在 IR 阶段直接替换成常量。
- 对数组步长、固定维度乘积等值，在进入循环前计算一次。

收益预期：中到高。风险中等，需要先建立循环结构分析，不能简单按文本模式替换。

## 6. 恢复函数内联，但只做严格安全版本

之前的错误来自不安全循环优化，不是内联本身。性能用例中很多小函数如 `idx()`、简单算术辅助函数，调用频率很高，内联收益明显。

建议只允许以下函数内联：

- 非递归。
- 非 `void`。
- 函数体只有一个 `return expr;`。
- `expr` 不包含函数调用、不包含数组写入、不包含全局变量写入。
- 参数只读，且不引入别名写入问题。

第一阶段可以只内联纯表达式函数，例如 `return r * n + c;`。

收益预期：高。风险中等，必须先加递归检测和副作用检测。

## 7. 针对整型除法和取模做通用强度削弱

很多代码有 `% 常量`、`/ 常量`。RISC-V 上 `divw/remw` 很慢。

建议先处理安全简单场景：

- 除数为 2 的幂且被除数已知非负时，`/` 用算术或逻辑右移，`%` 用 `andi`。
- 对一般正整数常量，可以后续实现 magic number 除法，但要严格处理 SysY 的 C 风格有符号除法语义。

收益预期：中到高。风险中等，尤其负数行为容易错。

## 8. 改善函数调用约定下的参数处理

当前函数入口会立刻把所有参数存入栈槽，之后再从栈槽加载。对于只读、短生命周期参数，可以直接保留在寄存器缓存中。

建议：

- 对不被取地址、不被赋值的标量参数，入口先不落栈。
- 参数第一次需要内存地址时再 materialize 到栈槽。
- 对数组参数，保留指针寄存器，循环里避免重复 `ld`。

收益预期：中。风险中等，需要和现有 `alloca/load/store` 模型配合。

## 9. 输出汇编 peephole 优化

在生成完单个函数后，对汇编文本做局部 peephole，不需要复杂 IR 分析：

- 删除 `j next_label` 且下一行正好是 `next_label:` 的跳转。
- 合并 `li reg, imm` 后立即 `mv other, reg` 的场景。
- 删除连续重复加载同一栈槽到同一寄存器且中间没有写该栈槽的指令。
- 删除调试注释 `# arg...`，减少汇编体积，虽然运行时间收益小，但编译和汇编阶段更轻。

收益预期：低到中。实现风险低。

## 10. 不建议优先做的优化

以下优化短期风险较高，不建议在当前 AC 基线上先做：

- 跨基本块全局寄存器分配。
- 复杂循环重写和数组填充识别。
- 自动向量化。
- 浮点表达式重关联。
- 基于测试点文件名、函数名、输入规模的特判。

## 推荐实施顺序

1. GEP 的 `slli` 和常量偏移优化。
2. 基本块内寄存器缓存，只处理 `i32` 临时值。
3. use-count 驱动的临时值不落栈。
4. 删除无用跳转和简单汇编 peephole。
5. 严格安全的小函数内联。
6. 循环不变量外提。
7. 常量除法/取模强度削弱。

每完成一步，都保存一个归档版本，并记录性能变化和是否仍 AC。
