如果你想真正变成：

SysY -> AST -> LLVM-like IR -> 自研 RISC-V

还需要做一层内部 lowerer：
  1. 把当前 generate_program_ir() 不只是输出文本，而是构造内存中的 IR 指令结构。
  2. 让 RISC-V 后端读取 IR 指令，而不是读取 AST。
  3. 覆盖至少这些 IR 指令：
      - alloca/load/store
      - add/sub/mul/sdiv/srem
      - fadd/fsub/fmul/fdiv
      - icmp/fcmp/zext
      - sitofp/fptosi
      - br/ret/call
      - getelementptr/bitcast
      - 全局变量和数组初始化
  4. 保留当前 ABI 逻辑：a0-a7、fa0-fa7、栈参数、lla 全局地址。

  不能用 llc，因为平台明确禁止运行时调用外部编译器工具。