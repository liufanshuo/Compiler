• 你这个 目标代码生成/ 里的编译器，实际源码语言是 SysY2022，目标是 RISC-V 64 汇编。主流程不是“SysY -> LLVM IR -> RISC-V”，而是现在直接走：

  SysY 源码 -> Flex 词法分析 -> Bison 语法分析/AST -> 符号表/常量求值 -> 直接生成 RISC-V 汇编

  核心文件

  - 目标代码生成/Lexer.l:34：词法规则，把 int、while、数字、标识符、运算符等识别成 token。
  - 目标代码生成/Parser.y:77：语法规则，把 token 规约成 AST，最后写入全局 g_program。
  - 目标代码生成/compiler.h:7：AST 数据结构定义，比如 Expr、Stmt、Decl、FuncDef、Program。
  - 目标代码生成/compiler.c:4136：主实现，包含 AST 构造、符号表、常量求值、RISC-V 汇编生成。
  - 目标代码生成/Makefile:1：用 gcc 编译 parser.tab.c、lex.yy.c、compiler.c 生成 compiler。

  入口流程
  main 在 目标代码生成/compiler.c:4136：

  1. parse_input_path 找输入 .sy 文件。
  2. parse_output_path 找 -o 后面的输出 .s 文件。
  3. yyin = fopen(input_path, "r")。
  4. yyparse() 调 Bison 解析，构造 AST。
  5. generate_program_asm(g_program你这个 目标代码生成/` 里的编译器主线是：

  SysY2022 源码 -> Flex 词法分析 -> Bison 语法分析 -> AST -> 直接生成 RISC-V 汇编

  注意：代码里保留了一套 LLVM IR 生成函数，但当前真正运行的路径不是 LLVM IR，而是直接输出 RISC-V 汇编。

  核心文件

  - 目标代码生成/Lexer.l:34：词法分析，把 int、while、标识符、整数、浮点、字符串、运算符等识别成 token。
  - 目标代码生成/Parser.y:77：语法分析，把 token 组合成 AST，最终写入全局变量 g_program。
  - 目标代码生成/compiler.h:7：定义 AST 数据结构，例如 Expr、Stmt、Decl、FuncDef、Program。
  - 目标代码生成/compiler.c:278：实现 AST 构造、符号表、常量求值、RISC-V 汇编生成。
  - 目标代码生成/compiler.c:4136：main 入口，解析命令行参数，调用 yyparse()，然后调用 generate_program_asm() 输出汇编。

  运行入口
  main 做的事很简单：

  1. 从命令行里找输入 .sy 和 -o 后面的输出 .s。
  2. 打开源文件。
  3. 调用 yyparse() 解析源码，构造 AST。
  4. 打开输出文件。
  5. 调用 目标代码生成/compiler.c:4094 生成 RISC-V 汇编。

  也就是说评测命令：

  compiler -S -o testcase.s testcase.sy

  实际会走到：

  yyparse();
  generate_program_asm(g_program, out);

  AST 怎么理解
  compiler.h 是最适合先看的。它把 SysY 程序抽象成几类节点：

  - Program：整个程序。
  - TopLevelItem：顶层元素，可能是全局变量声明，也可能是函数定义。
  - Decl / DeclItem：变量、常量、数组声明。
  - FuncDef：函数定义，包含返回类型、函数名、参数列表、函数体。
  - Block：语句块。
  - Stmt：语句，包括赋值、表达式语句、块、if、while、break、continue、return、printf。
  - Expr：表达式，包括整数、浮点、左值、函数调用、一元运算、二元运算。

  比如：

  int main() {
      return 3;
  }

  大致会被解析成：

  Program
    TopLevelItem: FuncDef main
      ret_type: int
      params: empty
      block:
        Stmt RETURN
          Expr NUMBER 3

  语法分析部分
  目标代码生成/Parser.y:356 里每条语法规则都会调用 make_xxx 构造 AST。

  例如 return：

  RETURNTK Exp SEMICN
      { $$ = make_return_stmt($2); }

  表达式优先级也在这里体现：

  - MulExp：* / %
  - AddExp：+ -
  - RelExp：< > <= >=
  - EqExp：== !=
  - LAndExp：&&
  - LOrExp：||

  所以看表达式语法，建议从 目标代码生成/Parser.y:478 开始看。

  汇编生成部分
  真正的后端从 目标代码生成/compiler.c:4094 开始：

  1. 先收集所有函数信息，建立函数符号表。
  2. 再生成全局变量，放到 .data 或 .bss。
  3. 最后逐个函数生成 .text 代码。

  函数生成在 目标代码生成/compiler.c:4042。它会：

  - 建立函数作用域。
  - 给参数分配栈空间。
  - 按 RISC-V ABI 从 a0-a7、fa0-fa7 或栈上传入参数。
  - 生成函数体。
  - 统一跳到 return label。
  - 输出函数序言和尾声：

  addi sp, sp, -frame_size
  sd ra, ...
  sd s0, ...
  addi s0, sp, frame_size
  ...
  ld ra, ...
  ld s0, ...
  addi sp, sp, frame_size
  ret

  表达式生成
  表达式生成核心是 目标代码生成/compiler.c:3002。

  这个后端约定：表达式结果默认放在 a0。

  例如：

  - 整数常量：li a0, value
  - 变量读取：算地址，然后 lw a0, 0(addr)
  - 函数调用：设置参数寄存器，然后 call
  - 整数加法：addw a0, lhs, rhs
  - 比较：用 slt、seqz、snez
  - 浮点：用 ft0/ft1/fa0，但中间结果常用 a0 保存浮点 bit pattern

  条件表达式在 目标代码生成/compiler.c:3115，这里专门处理 &&、|| 的短路逻辑。

  语句生成
  语句生成核心是 目标代码生成/compiler.c:3834。

  常见语句对应关系：

  - 赋值：先算左值地址，再算右值，最后 sw。
  - if：生成 if_then、if_else、if_end 标签。
  - while：生成 while_cond、while_body、while_end 标签。
  - break / continue：通过 break_labels、continue_labels 栈找到跳转目标。
  - return：把返回值放进 a0 或 fa0，然后跳到统一 return label。
  - printf：拆成 putint、putch、putfloat 运行时库调用。

  数组处理
  这个编译器把数组“扁平化”存储。

  例如：

  int a[2][3];

  内部按连续 6 个 int 处理。访问 a[i][j] 时会算线性下标：

  index = i * 3 + j
  address = base + index * 4

  - 简单函数内联：目标代码生成/compiler.c:2723
  - 局部简单赋值块 DAG 优化：目标代码生成/compiler.c:3705

  建议你按这个顺序读：

  1. compiler.h：先搞清楚 AST 结构。
  2. Lexer.l：知道 token 怎么来。
  3. Parser.y：知道 SysY 语法怎么变成 AST。
  4. compiler.c 的 main 和 generate_program_asm：建立整体流程。
  5. asm_gen_function、asm_gen_block、asm_gen_stmt、asm_gen_expr：这是目标代码生成核心。
  6. 最后再看优化函数和数组、浮点、函数调用细节。

  我还用 00_main.sy 跑了一下，return 3; 会生成典型的 RISC-V 函数框架和 li a0, 3，说明当前主路径确实是直接生成 .s 汇编。