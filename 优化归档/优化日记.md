## 2026-05-24 轻量公共子表达式删除调整

### 本次修改

- 修改文件：`/home/vance/Compiler/目标代码生成/compiler.c`
- 将上一版基本块 DAG 路径收窄为更轻量的公共子表达式删除：
  - 只有检测到重复的一元/二元表达式时才启用 DAG 路径，纯覆盖赋值不再触发。
  - 为 DAG 节点增加引用计数，只把会被复用的中间表达式保存到临时栈槽。
  - 无覆盖且不存在后续旧值依赖风险时，表达式结果直接写回目标变量，避免额外 result slot。
  - DAG 发射常量操作数时复用已有立即数优化，减少 `x + const` 等场景退化为通用 `li/addw` 序列。
- 追加常数折叠与常数传播触发：
  - 在连续本地 `int` 标量赋值块中，`x = 3; y = x + 1;` 可传播为常量节点并进一步折叠。
  - 一元/二元表达式被折叠为常量，或被 `+0`、`*1`、`*0`、`/1`、`%1` 等规则简化时，也允许启用轻量 DAG 路径。
  - 若没有公共子表达式但确实发生常量传播/折叠，不再回退到普通逐语句生成。

### 验证说明与风险

- 本地按交接约束只做静态检查：
  - `git diff --check -- 目标代码生成/compiler.c`
  - 扫描确认未新增 `system`、`popen`、`fork`、`exec` 等外部工具调用。
- 已重新生成 `目标代码生成/compiler.zip`，内容为 `compiler.c`、`compiler.h`、`lex.yy.c`、`parser.tab.c`、`parser.tab.h`。
- 未运行 `gcc`、`clang`、`make`、`flex`、`bison`、RISC-V 工具链或 `./compiler`。
- 尚无平台测评时间；需要提交平台后判断该轻量 CSE 是否优于上一版 `2292.3322s`。

## 2026-05-22 14:22 agent2 合规检查与优化记录

### agent1 实际修改

- 修改文件：`/home/vance/Compiler/目标代码生成/compiler.c`
- 短路逻辑表达式 `&&` / `||` 求值不再分配临时栈槽，也不再通过 `sw` / `lw` 保存布尔结果，改为在分支标签中直接设置 `a0` 为 `1` 或 `0`。
- 条件跳转生成增加整数常量条件识别：可静态求值的条件直接跳转到 `true_label` 或 `false_label`，避免生成表达式求值和 `bnez`。
- `while` 入口删除跳到紧随其后条件标签的冗余 `j while_cond`。

### 平台要求核对

- 实现语言仍为 C，符合平台允许的 C/C++/Java/Rust 范围。
- `main` 函数和命令行输出路径逻辑未修改，仍应支持 `compiler -S -o testcase.s testcase.sy`。
- 末尾 `-O1` 会作为以 `-` 开头的选项被忽略，不影响输入文件识别。
- 输出仍写入 `-o` 指定路径，目标仍为 64 位 RISC-V 汇编生成流程。
- 本次优化属于通用目标代码生成优化，未发现针对特定用例名、函数名、固定输出或平台探测的行为。

### 验证说明

- 本阶段仅做静态合规检查和记录整理。
- 未运行外部编译器或目标程序。
- agent1 确认未调用 `gcc`、`clang`、`g++`、`make`、`flex`、`bison`、RISC-V 工具链或 `./compiler`。
- 测评平台归档：`/home/vance/Compiler/优化归档/2026_5_22_1600_2289.3332s`
- 平台执行时间：`2289.3332s`

### 残余风险

- 未实际编译运行验证，仍存在实现细节回归未被动态测试覆盖的风险。
- `目标代码生成` 目录内存在预编译 `compiler` 和 `compiler.zip` 额外产物；按平台源码扫描逻辑一般不影响，但最终提交前建议确认平台是否允许保留。
- 当前归档目录中保存的是 `compiler.zip` 提交包；若需要逐轮源码 diff，建议后续同时归档展开后的源码目录。

## 2026-05-22 17:00 基本块内 DAG 优化记录

记录写入时间：`2026-05-24 18:30`

### 本次修改

- 修改文件：`/home/vance/Compiler/目标代码生成/compiler.c`
- 优化内容：在目标代码生成阶段加入基本块内三地址值建模和 DAG 优化。
- 实现范围：只处理连续的本地 `int` 标量赋值语句，先构建 DAG 节点，再生成 RISC-V。
- 已覆盖的基本块内优化：
  - 常量折叠，例如纯常量二元表达式直接计算。
  - 代数化简，例如 `x + 0`、`x - 0`、`x * 1`、`x * 0`、`x / 1`、`x % 1`。
  - 公共子表达式复用，对重复的一元/二元表达式只生成一次中间结果。
  - 同一基本块内覆盖赋值消除，仅保留变量在块尾需要写回的最后值。

### 平台测评结果

- 测评平台归档目录：`/home/vance/Compiler/优化归档/2026-5-22-1700-2292.3322s`
- 归档内容：`compiler.zip`
- 平台执行时间：`2292.3322s`
- 相比上一条保留记录的 `2289.3332s`，时间增加约 `2.9990s`，本次 DAG 优化在该测评结果下未体现性能收益。

### 合规检查

- 本次优化属于通用基本块内优化，不依赖特定用例名、文件名、函数名、固定输出或平台探测。
- 未在编译器代码中加入 `system`、`popen`、`fork`、`exec` 或调用 `gcc`、`clang`、`make`、RISC-V 工具链等外部工具的逻辑。
- 对数组、全局变量、函数调用、浮点、短路逻辑、控制流和 I/O 语句采用保守回退，不进入 DAG 优化路径。
- 命令行入口、`-o` 输出路径处理、`-S`/`-O1` 兼容逻辑未修改。

### 验证说明与风险

- 本地未运行外部编译器、构建器、RISC-V 工具链或 `./compiler`，只做静态 diff 和合规检查。
- 当前测评结果显示性能变差，后续应优先分析 DAG 路径是否引入了过多临时栈槽和额外 `lw/sw`。
