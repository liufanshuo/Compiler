# AST->LLVM->RISCV 优化日记

## 2026-05-28 16:19 CST

### 首次记录：恢复正确性的基础优化

本次目标是将目标代码生成路径稳定为 `AST -> LLVM 风格内部 IR -> RISC-V`，并排除原先 `AST -> RISC-V` 直出后端对当前结果的影响。旧的 AST 直出 RISC-V 代码已经删除，默认汇编生成路径现在只经过 `ast_to_ir()` 和 `emit_riscv_from_ir()`。

决赛性能用例出现大面积 WA，其中大量实际输出哈希为 `68b329da9893e34099c7d8ad5cb9c940`，说明程序运行结果高度异常。排查后确认一个通用语义错误：LLVM IR 生成阶段存在“数组填充 while 循环”快捷优化，会把特定嵌套循环替换成线性填充，但没有维护原循环变量在循环结束后的最终值。SysY 中循环变量在 `while` 后继续使用是合法行为，该优化会把这些变量错误地保留在初始值，导致后续控制流或计算结果错误。

处理方式是删除该不安全优化，让 `mem_gen_block()` 按 AST 原始块顺序生成 LLVM 风格内部 IR，所有 `while` 循环继续走正常控制流生成。该修改不针对任何特定测试点、函数名或输入输出数据，也不调用外部编译器工具。

结果：修复后当前提交在评测中 AC。这个版本应作为 LLVM->RISC-V 路线的第一个稳定正确性基线。

后续优化原则：

- 先保持正确性基线，任何 IR 级优化都必须证明不改变可观察语义。
- 循环优化必须维护循环变量、break/continue、数组写入、函数调用副作用等状态。
- 不做测试点名、函数名、输入输出内容相关的特判。
- 不在编译器代码中调用 clang/gcc/llc/opt/as 等现有编译器或汇编工具。


## 2026-05-28 20:00 CST
已完成通用优化，遵守 harness.md 的约束，没有按用例名/函数名特判，也没有在编译器代码中调用外部编译器工具。

  主要改动在 目标代码生成/compiler.c:4530：

  - 新增常量识别和 2 的幂识别 helper。
  - 优化 目标代码生成/compiler.c:4812：
      - add/sub 常量改用 addiw
      - mul x, 0/1/-1/2^k 改成更短指令
      - div/rem 对 1/-1 做安全化简
  - 优化 目标代码生成/compiler.c:4985：
      - 常量下标直接折算偏移
      - 动态下标遇到 2 的幂 stride，用 slli 替代 li + mul

  静态效果，60 个 RISCV 决赛性能用例生成汇编统计：

  - 总行数：162843 -> 161064
  - li：5148 -> 3372
  - mul/mulw：1350 -> 366
  - 新增：slli 804、slliw 177、addiw 759

  验证结果：

  - make 通过，仅有原本 lex.yy.c 的两个 warning。
  - git diff --check 通过。
  - 公开功能/性能用例和 60 个决赛性能用例均可成功生成汇编。
  - 所有生成汇编均通过 clang -target riscv64-linux-gnu -c 语法检查。
  - 本机没有 qemu-riscv64，所以无法本地直接跑 RISC-V 可执行输出。
  - 已更新 目标代码生成/compiler.zip，并通过 unzip -t 校验。

## 2026-05-28-22:00 CST
已实现第一版 IR 基本块内优化，并接入 AST -> LLVM IR -> optimize_ir_basic_blocks() -> RISC-V 路径。

  主要改动在 目标代码生成/compiler.c:3657 到 目标代码生成/compiler.c:4498：

  - 常量折叠：3 + 4 -> 7，支持 add/sub/mul/sdiv/srem 的整数常量折叠。
  - 代数化简：x + 0、x - 0、x * 1、x * 0、x / 1、x % 1、x - x。
  - icmp 化简：常量比较、icmp x, x。
  - 冗余 zext/bitcast 消除。
  - 基本块内简单 CSE：只对纯表达式做，不碰 load/store/call/alloca/br/ret。
  - 死纯指令删除：替换后无 use 的纯 IR 指令会从基本块链表移除。
  - 编译路径已在 目标代码生成/compiler.c:5902 调用 optimize_ir_basic_blocks(module)。

  ## 2026-05-29-11:00 CST
  已完成一版全局 IR 优化，并遵守 harness.md：没有外部编译器调用、没有用例名/函数名特判、没有预置输出。

  实现内容在 目标代码生成/compiler.c:4626 附近：

  - 全局常量/拷贝传播：通过 IR value 替换和迭代简化完成。
  - 死代码删除：纯指令 DCE、终结符 ret/br 后不可达指令删除。
  - 简单 copy propagation：冗余 zext/bitcast、代数化简、load 转发后的 value 替换。
  - load/store 局部转发：目标代码生成/compiler.c:4724。
  - 保守 mem2reg 子集：目标代码生成/compiler.c:4626。
      - 只提升标量 alloca。
      - 要求地址不逃逸，只被直接 load/store 使用。
      - 单 store 必须支配所有 load。
      - 分支汇合需要 phi 的情况暂不冒险处理。

  优化入口已改为 目标代码生成/compiler.c:4838，当前编译链路仍是 AST -> LLVM IR -> optimize_ir -> RISC-V。
  ## 2026-5-29-12：00 CST
  已完成一版保守循环优化，仍遵守 harness.md：没有外部编译器调用、没有用例名/函数名特判、没有预置输出。

  主要新增在 目标代码生成/compiler.c:5019：

  - 自然循环识别：用 dominator + backedge 找循环。
  - 循环不变量外提 LICM：目标代码生成/compiler.c:5019。
  - 简单循环内 CSE：目标代码生成/compiler.c:5044。
  - 循环 pass 入口：目标代码生成/compiler.c:5079。
  - 增强了基本块内 load 转发：目标代码生成/compiler.c:4724，同一基本块重复读取同一标量栈槽会复用第一次 load 的 IR value。

  安全边界：

  - 只移动/复用纯指令。
  - 不外提 load/store/call/div/rem。
  - 不改 CFG，不生成 phi。
  - 地址递推/完整归纳变量强度削弱没有硬做，因为当前 IR 还不是完整 SSA，直接做指针递增很容易在分支、continue、嵌套循环里错。
  ## 2026-5-29-00:00 CST
  已改成非递归的局部寄存器缓存/调度，并完成验证。

  实现方式是保守的基本块内线性寄存器计划，不再递归展开表达式：

  - 新增 RVRegTemp，记录某个 SSA 临时值绑定到 t3/t4。
  - 只缓存满足这些条件的值：
      - 整数/指针结果，不缓存 float；
      - 定义和唯一使用在同一个基本块；
      - 定义到使用之间没有 call；
      - 当前 t3/t4 有空闲寄存器；
      - 支持的指令包括 load/add/sub/mul/div/rem/icmp/zext/gep/bitcast。
  - 使用点之后线性释放寄存器。
  - 不使用 t6，因为现有后端会用 t6 做 PHI copy 和浮点常量临时寄存器，缓存到 t6 有隐藏风险。
  - 不再用之前出错的递归内联路径，因此不会覆盖嵌套表达式的操作数。

  关键位置：

  - 目标代码生成/compiler.c:6000：新增 RVRegTemp。
  - 目标代码生成/compiler.c:6177：判断可缓存的临时值类型。
  - 目标代码生成/compiler.c:6200：查找唯一使用点。
  - 目标代码生成/compiler.c:6248：基本块内线性分配 t3/t4。
  - 目标代码生成/compiler.c:6298：结果写入寄存器缓存而不是栈槽。
  - 目标代码生成/compiler.c:6334：读取缓存值时直接 mv。

  ## 2026-5-30-01：00 CST
  实现这个 IR peephole，改在 目标代码生成/compiler.c:4130：

  - icmp ne (zext i1 X to i32), 0 直接替换为 X
  - icmp eq (zext i1 X to i32), 0：
      - 如果 X 是 icmp，直接反转谓词，比如 slt -> sge，让后端现有 compare-branch fusion 能生成 blt/bge/... + j
      - 否则退化为 icmp eq i1 X, 0

  同时调整了基本块优化流程，让这种“原地改写 icmp”的情况也会被计入 changed，并继续触发 DCE/CSE，见 目标代码生成/compiler.c:4562。

  实现常量强度削弱，主要在 目标代码生成/compiler.c:6272、目标代码生成/compiler.c:6700、目标代码生成/compiler.c:6787：

  - x + imm12、x - imm12：走 addiw
  - x * 2^k：走 slliw
  - x / 2^k：走 sraiw + bias 修正，保持有符号向零截断
  - x % 2^k：走 sign mask + and/sub 模式，保持负数余数语义
  - x < imm12：物化比较时走 slti
  - x == 0 / x != 0：直接走 seqz / snez
  - 额外覆盖了 x <= imm、x > imm、x >= imm 在安全立即数范围内的 slti + xori

  